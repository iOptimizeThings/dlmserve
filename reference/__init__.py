"""Vendored reference implementations used as ground truth for reference-match testing.

Nothing in this package is imported by the runtime engine. It exists only so
that tests can pin our behavior to a published implementation byte-for-byte.
"""
